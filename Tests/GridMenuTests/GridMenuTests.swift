import XCTest
@testable import GridMenu

final class GridMenuTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(GridMenu().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
